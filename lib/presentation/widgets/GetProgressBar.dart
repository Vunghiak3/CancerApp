import 'package:flutter/material.dart';

class GetProgressBar extends StatelessWidget {
  const GetProgressBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: CircularProgressIndicator(),
    );
  }
}
